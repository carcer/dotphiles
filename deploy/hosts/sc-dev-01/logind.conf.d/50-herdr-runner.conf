# Installed to /etc/systemd/logind.conf.d/ by deploy/configure-system.sh when
# the host profile sets DOTPHILES_ROLE=host. sc-dev-01 runs agent lanes
# unattended, so going idle (or closing a lid) must never suspend it.
[Login]
HandleLidSwitch=ignore
HandleLidSwitchExternalPower=ignore
HandleLidSwitchDocked=ignore
IdleAction=ignore
HandlePowerKey=poweroff
