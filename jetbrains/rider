#!/bin/bash
#Generated by JetBrains Toolbox 3.7.2.87231 at 2026-08-23T11:04:58

declare -a intellij_args=()
declare -- wait=""

for o in "$@"; do
  if [[ "$o" = "--wait" || "$o" = "-w" ]]; then
    wait="-W"
    o="--wait"
  fi
  if [[ "$o" =~ " " ]]; then
    intellij_args+=("\"$o\"")
  else
    intellij_args+=("$o")
  fi
done

open -na "/Users/chris/Applications/Rider.app/Contents/MacOS/rider" $wait --args "${intellij_args[@]}"